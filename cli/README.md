# skillforge (CLI)

Find and install AI agent skills from the SkillForge registry.

```bash
npx skillforge find "pdf forms"
npx skillforge add owner/repo/skill-path
npx skillforge list
```

See the main repo README for the full project.
